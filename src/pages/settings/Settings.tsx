import React from "react";
import { StyledSettings } from "./Settings.styled";

function Settings() {
  return (
    <StyledSettings>
      <h1>Settings</h1>
    </StyledSettings>
  );
}

export default Settings;
